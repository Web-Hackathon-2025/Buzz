// If you want strict typing, you can install: npm install -D postcss-load-config
// and use: import type { Config } from 'postcss-load-config'

export default {
    plugins: {
      '@tailwindcss/postcss': {},
      'autoprefixer': {},
    },
  }